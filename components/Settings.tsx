import Signout from "./Signout";

const Settings = () => {
    return (
        <div className="p-2 md:p-0 w-full flex justify-end">
            <Signout />
        </div>
    );
};

export default Settings;
