import BookshelfDisplay from "@/components/BookshelfDisplay";

const bookCorner = () => {
    return (
        <section>
            <BookshelfDisplay />
        </section>
    );
};

export default bookCorner;
